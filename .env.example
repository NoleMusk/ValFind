PORT=3000
NODE_ENV=development

# Local PostgreSQL
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=seattle_search

# Hosted PostgreSQL
# On Render/Supabase/Neon you can usually use a single connection string instead.
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE
